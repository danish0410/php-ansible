apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-ebs-pvc
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp2
